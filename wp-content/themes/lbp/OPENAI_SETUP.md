# Настройка OpenAI API для автосортировки слов

## Получение API ключа

1. Зарегистрируйтесь на https://platform.openai.com/
2. Перейдите в раздел API keys
3. Создайте новый ключ (начинается с `sk-...`)

## Настройка в WordPress

### Вариант 1: Через wp-config.php (рекомендуется)

Добавьте в файл `wp-config.php` (перед строкой `/* That's all, stop editing! */`):

```php
define('OPENAI_API_KEY', 'sk-ваш-ключ-здесь');
```

### Вариант 2: Через базу данных

Выполните SQL запрос:

```sql
INSERT INTO wp_options (option_name, option_value, autoload) 
VALUES ('openai_api_key', 'sk-ваш-ключ-здесь', 'yes')
ON DUPLICATE KEY UPDATE option_value = 'sk-ваш-ключ-здесь';
```

## Использование

1. Откройте категорию в словаре
2. Нажмите "🔄 Порядок слов"
3. Нажмите "🤖 Упорядочить ботом"
4. AI отсортирует слова по смыслу и частоте использования
5. Нажмите "Сохранить"

## Стоимость

Используется модель `gpt-4o-mini`:
- ~$0.00015 за 1000 токенов входа
- ~$0.0006 за 1000 токенов выхода
- Сортировка 50 слов ≈ $0.001 (менее 1 цента)

## Безопасность

⚠️ **Не коммитьте API ключ в Git!**
Добавьте в `.gitignore`:
```
wp-config.php
```

